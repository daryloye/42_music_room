# 42_music_room

## Contributor setup

Run the one-time setup:

```sh
make setup
```

This installs dependencies and `goimports`, then enables the pre-commit format hook.
VS Code will recommend the Go and Prisma extensions; extensions that were manually disabled must also be manually enabled.
