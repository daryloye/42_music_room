info:
  name: main
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/auth/signup
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: '{email:"test", password:"test"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​