meta {
  name: 00_healthcheck
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: inherit
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
