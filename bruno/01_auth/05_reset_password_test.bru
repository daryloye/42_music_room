meta {
  name: 05_reset_password_test
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/auth/reset-password
  body: json
  auth: inherit
}

body:json {
  {
    "token": "invalid",
    "password": "invalid"
  }
}

tests {
  test("status is 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
