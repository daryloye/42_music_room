meta {
  name: 01_verify_test
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/auth/verify
  body: json
  auth: inherit
}

body:json {
  {
    "token":"123"
  }
}

tests {
  test("status is 404", function() {
    expect(res.getStatus()).to.equal(404);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
